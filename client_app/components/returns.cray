 /** @jsx React.DOM */
var React = require('react');

var Returns = React.createClass({
  render: function(){
    return(
      <h3>
        I am the Returns Component.
      </h3>
    )
  }
})

module.exports = Returns;